import React from "react";
import styled from "styled-components";

const Tips = () => {
  return (
    <Wrapper>
      Tips
      {/* <>
        Feet on the ground Chest up Pull shoulderblades back and in Bar above
        eyes on the rack Bar hits chestbone no bounce Push it up to straight
        arms, use with control Just outside shoulder width
      </> */}
    </Wrapper>
  );
};

const Wrapper = styled.div``;

export default Tips;
